# 연결 업데이트 기능 완벽 가이드

## 개요

**연결 업데이트**는 Visual Canvas Board의 핵심 AI 기능으로, 연결선으로 연결된 항목들의 맥락을 자동으로 분석하여 초안 텍스트를 업데이트합니다. 단순한 텍스트 확장을 넘어, 이미지 분석, 선택, 생성까지 가능한 강력한 기능입니다.

## 기본 사용법

### 1. 연결 만들기

```
초안 텍스트 → (연결선) → 참고 항목
```

1. 초안 텍스트 항목 선택
2. 연결 핸들(작은 원)을 드래그하여 참고 항목에 연결
3. 연결선을 더블클릭하여 **라벨 입력**
4. 왼쪽 툴바에서 AI 버튼(✨) 클릭 → "연결 기반 업데이트" 선택

### 2. 연결 라벨의 중요성

**라벨이 작업을 결정합니다!** AI가 라벨의 키워드를 분석하여 수행할 작업을 판단합니다.

---

## 기능 1: 텍스트 기반 작업

### A) 요약

**라벨**: "요약", "간단히", "핵심만"

**동작**: 연결된 텍스트 내용을 요약하여 초안에 추가

**예시**:

```
초안: "제품 소개"
  ↓ (라벨: "요약")
참고: "긴 제품 설명 텍스트..."

결과: 초안에 핵심 내용만 요약되어 추가됨
```

### B) 확장

**라벨**: "확장", "자세히", "구체적으로", "더 설명"

**동작**: 연결된 내용을 바탕으로 초안을 더 상세하게 확장

**예시**:

```
초안: "키보드 종류"
  ↓ (라벨: "확장해줘")
참고: "기계식, 멤브레인, 광축..."

결과: 각 종류에 대한 상세 설명 추가
```

### C) 보충 설명

**라벨**: "보충", "추가 정보", "참고"

**동작**: 연결된 정보를 참고하여 관련 내용 추가

**예시**:

```
초안: "사용자 리뷰"
  ↓ (라벨: "참고해서 작성")
댓글 텍스트들

결과: 리뷰 내용을 반영한 분석 텍스트
```

---

## 기능 2: 이미지 선택 (기존 이미지 활용)

### 키워드

- "선택", "골라", "고르", "찾아"
- "어울리는", "적합한", "맞는"

### 작동 방식

1. **이미지 연결** (개별 또는 그룹)
2. **라벨 입력**: "어울리는 이미지 골라서 넣어줘"
3. **AI 분석**:
   - Gemini 2.5 Flash가 모든 이미지를 **multimodal 분석**
   - 텍스트 맥락과 가장 어울리는 이미지 선택
4. **자동 삽입**: 선택된 이미지들이 적절한 위치에 삽입됨

### 예시 시나리오

**상황**: 블로그 포스트 "기계식 키보드의 매력"

```
초안 텍스트
  ↓ (라벨: "가장 어울리는 이미지를 찾아서 넣어줘")
[그룹: 키보드 이미지 10개]
```

**AI 분석**:

- 이미지 0: 화이트 키보드 → "깔끔한 디자인" 문단에 적합
- 이미지 3: RGB 키보드 → "게이밍 키보드" 문단에 적합
- 이미지 7: 파스텔 키보드 → "데스크테리어" 문단에 적합

**결과**:

```json
{
  "content": "# 기계식 키보드의 매력\n\n깔끔한 디자인... {{EXISTING_IMAGE_0}} ...\n\n게이밍 키보드... {{EXISTING_IMAGE_3}} ...\n\n데스크테리어... {{EXISTING_IMAGE_7}}",
  "selectedImages": [
    {
      "position": 0,
      "imageIndex": 0,
      "reason": "화이트 키보드가 깔끔한 디자인 설명에 적합"
    },
    {
      "position": 1,
      "imageIndex": 3,
      "reason": "RGB 조명이 게이밍 키보드 설명과 잘 어울림"
    },
    {
      "position": 2,
      "imageIndex": 7,
      "reason": "파스텔 컬러가 데스크테리어 컨셉과 일치"
    }
  ]
}
```

### 지원하는 연결 방식

1. **개별 이미지 여러 개 연결**

   ```
   초안 → 이미지1
        → 이미지2
        → 이미지3
   ```

2. **그룹 박스에 연결** (권장)

   ```
   초안 → [그룹 박스: 이미지 5개]
   ```

3. **혼합 연결**
   ```
   초안 → 이미지A (개별)
        → 이미지B (개별)
        → [그룹: 이미지C, D, E]
   ```

---

## 기능 3: 이미지 생성 (새로운 이미지 생성)

### 키워드

- "생성", "만들", "만들어"
- "프롬프트", "새로운"

### A) 단순 이미지 생성

**라벨**: "이미지 프롬프트 3개 생성"

**동작**:

1. AI가 텍스트 맥락을 분석
2. 적절한 이미지 프롬프트 3개 생성
3. Imagen 4.0으로 실제 이미지 생성 (5-10초/개)
4. 생성된 이미지를 텍스트에 삽입

**예시**:

```
초안: "커피 브랜드 소개"
  ↓ (라벨: "이미지 3개 만들어줘")
(연결 없음)

결과:
- 이미지 1: 커피 원두 클로즈업
- 이미지 2: 라떼 아트
- 이미지 3: 카페 인테리어
```

### B) 레퍼런스 기반 이미지 생성 ⭐ (핵심 기능)

**라벨**: "이미지 참고해서 비슷한 스타일로 3개 만들어줘"

**동작**:

1. **연결된 이미지들을 multimodal 분석**:

   - 색상 팔레트 (파스텔, 비비드, 모노톤 등)
   - 조명 스타일 (soft lighting, dramatic shadows 등)
   - 디자인 컨셉 (minimalist, retro, futuristic 등)
   - 촬영 구도 (top-down, close-up, wide angle 등)

2. **스타일을 반영한 상세 프롬프트 생성**:

   ```
   "mechanical keyboard with pastel pink and mint green keycaps,
   soft lighting, minimalist desk setup, product photography style,
   shallow depth of field, kawaii aesthetic"
   ```

3. **Imagen 4.0으로 스타일 일관된 이미지 생성**

4. **텍스트에 자동 삽입**

**예시 시나리오**:

```
초안: "우리 브랜드의 제품 라인업"
  ↓ (라벨: "이미지 참고해서 적절한 이미지 3장 만들어줘")
[그룹: 파스텔 톤 제품 사진 4개]
```

**AI 분석 결과**:

- 색상: Pastel pink (#FFB6C1), Mint green (#B5E7A0), Lavender (#E6E6FA)
- 조명: Soft, diffused natural lighting
- 스타일: Minimalist, clean, kawaii aesthetic
- 구도: Top-down view, centered composition

**생성된 프롬프트**:

1. "product showcase with pastel pink packaging, soft natural lighting, minimalist white background, top-down view, commercial photography"
2. "collection of mint green accessories, clean aesthetic, gentle shadows, product photography style"
3. "lavender-themed items arranged minimally, soft focus background, pastel color palette"

**결과**: 기존 제품 사진들과 **완벽히 일치하는 스타일**의 새로운 제품 이미지 3개 생성

### 생성 시 주의사항

⚠️ **시간**: 이미지 1개당 5-10초 소요 (3개 = 15-30초)  
⚠️ **비용**: Imagen 4.0은 유료 API (이미지당 $0.02~$0.04)  
⚠️ **품질**: 참고 이미지가 많을수록 스타일 분석 정확도 향상

---

## 기능 4: 복합 작업

### 여러 항목 동시 연결

```
초안 텍스트
  ↓ (라벨: "참고")
텍스트A (요약할 내용)
  ↓ (라벨: "어울리는 이미지 골라줘")
[그룹: 이미지 5개]
  ↓ (라벨: "스타일 참고해서 2개 만들어줘")
[그룹: 레퍼런스 이미지 3개]
```

**AI가 자동으로**:

1. 텍스트A 내용 요약
2. 첫 번째 그룹에서 어울리는 이미지 선택
3. 두 번째 그룹 스타일 분석 후 새 이미지 2개 생성
4. 모든 내용을 통합하여 초안 업데이트

---

## 고급 활용 팁

### 1. 단계별 콘텐츠 제작

```
1단계: 아웃라인 생성 (AI 아웃라인 기능)
2단계: 각 섹션 초안 작성
3단계: 참고 자료 연결 → 연결 업데이트
4단계: 이미지 그룹 연결 → 이미지 선택/생성
5단계: 최종 리뷰 및 수정
```

### 2. 일관된 비주얼 스타일 유지

**시나리오**: 브랜드 가이드라인에 맞는 콘텐츠 제작

1. 브랜드 대표 이미지 3-5개를 그룹으로 만들기
2. 모든 콘텐츠 초안에서 이 그룹 연결
3. "이미지 참고해서 만들어줘" 사용
4. 결과: 브랜드 스타일과 일관된 이미지 생성

### 3. A/B 테스트용 콘텐츠

```
버전A 초안 → [밝은 톤 이미지 그룹] → 연결 업데이트
버전B 초안 → [어두운 톤 이미지 그룹] → 연결 업데이트
```

동일한 텍스트, 다른 비주얼 스타일로 2개 버전 생성

### 4. 다국어 콘텐츠 제작

```
한국어 초안
  ↓ (라벨: "영어로 번역하고 적절한 이미지 추가")
[글로벌 이미지 그룹]
```

번역 + 현지화된 이미지 선택을 한 번에

---

## 연결 라벨 예시 모음

### 텍스트 작업

- ✅ "요약해줘"
- ✅ "확장해서 3문단으로"
- ✅ "구체적인 예시 추가"
- ✅ "전문적인 톤으로 다듬어줘"
- ✅ "참고해서 보충 설명"

### 이미지 선택

- ✅ "어울리는 이미지 골라서 넣어줘"
- ✅ "적합한 이미지 3개 선택"
- ✅ "맞는 이미지 찾아서 추가"
- ✅ "관련 이미지 배치"

### 이미지 생성

- ✅ "이미지 프롬프트 3개 생성"
- ✅ "새로운 이미지 만들어줘"
- ✅ "참고 이미지 스타일로 2개 만들어줘"
- ✅ "비슷한 느낌으로 이미지 생성"
- ✅ "이미지 참고해서 적절한 이미지 4장 만들어줘"

### 복합 작업

- ✅ "요약하고 관련 이미지 추가"
- ✅ "확장하면서 이미지 3개 만들어줘"
- ✅ "스타일 참고해서 새로운 섹션 작성"

---

## 기술적 세부사항

### AI 모델 사용

| 작업             | 모델                          | 설명                     |
| ---------------- | ----------------------------- | ------------------------ |
| 텍스트 분석/생성 | Gemini 2.5 Flash              | 빠른 응답, 고품질 텍스트 |
| 이미지 분석      | Gemini 2.5 Flash (Multimodal) | 여러 이미지 동시 분석    |
| 이미지 생성      | Imagen 4.0                    | 최신 이미지 생성 모델    |

### 처리 흐름

1. **연결 정보 수집**:

   - `connectedItemsInfo`: 연결된 항목들의 메타데이터
   - `imageParts`: Base64 인코딩된 이미지 데이터
   - `imageSourceMap`: 이미지 인덱스 → 원본 매핑

2. **키워드 분석**:

   ```typescript
   hasSelectKeywords = /선택|골라|고르|찾아|어울리는|적합한|맞는/.test(label);
   hasGenerateKeywords = /생성|만들|프롬프트|새로운/.test(label);
   ```

3. **AI 프롬프트 생성**:

   - 맥락에 따라 동적으로 지침 생성
   - 이미지가 있으면 multimodal 콘텐츠 구성

4. **응답 처리**:

   - **JSON 응답**: `selectedImages` 또는 `images` 배열 처리
   - **텍스트 응답**: 마크다운 → HTML 변환

5. **결과 삽입**:
   - 플레이스홀더(`{{IMAGE_N}}`, `{{EXISTING_IMAGE_N}}`) 교체
   - Tiptap 에디터에 HTML 삽입

### 데이터 구조

**선택된 이미지 응답**:

```json
{
  "content": "텍스트 with {{EXISTING_IMAGE_0}}",
  "selectedImages": [
    {
      "position": 0,
      "imageIndex": 2,
      "reason": "선택 이유"
    }
  ]
}
```

**생성된 이미지 응답**:

```json
{
  "content": "텍스트 with {{IMAGE_0}}",
  "images": [
    {
      "position": 0,
      "prompt": "영어 프롬프트",
      "description": "한글 설명"
    }
  ]
}
```

---

## 제한사항 및 해결책

### 제한사항

1. **이미지 수 제한**: 한 번에 최대 ~10개 이미지 분석 권장 (API 용량 제한)
2. **생성 시간**: 이미지 1개당 5-10초 (많이 생성하면 오래 걸림)
3. **비용**: Imagen 4.0 유료 사용
4. **스타일 정확도**: 참고 이미지 품질에 따라 변동

### 해결책

1. **그룹 활용**: 관련 이미지들을 그룹으로 묶어서 연결
2. **점진적 생성**: 한 번에 2-3개씩 생성
3. **선택 우선**: 기존 이미지 활용 → 필요시에만 생성
4. **고품질 레퍼런스**: 명확한 스타일의 이미지 제공

---

## FAQ

**Q: 그룹과 개별 이미지 연결의 차이는?**  
A: 기능은 동일합니다. 그룹은 시각적으로 정리하고 한 번에 연결하기 편리합니다.

**Q: 이미지가 생성되지 않아요.**  
A:

1. API 키가 올바른지 확인
2. 라벨에 "생성", "만들" 키워드 포함
3. 콘솔에서 오류 메시지 확인

**Q: 선택된 이미지가 맥락과 안 맞아요.**  
A:

1. 더 구체적인 라벨 사용 ("디자인 섹션에 어울리는 이미지")
2. 연결된 이미지 수를 줄여서 선택 범위 좁히기

**Q: 생성된 이미지 스타일이 참고 이미지와 달라요.**  
A:

1. 참고 이미지들의 스타일을 일관되게 (모두 파스텔 톤, 모두 비슷한 구도)
2. 참고 이미지 개수 늘리기 (3-5개 권장)
3. 라벨에 스타일 명시 ("파스텔 톤으로 만들어줘")

**Q: 여러 작업을 한 번에 할 수 있나요?**  
A: 네! 여러 항목을 다른 라벨로 연결하면 AI가 모두 처리합니다.

---

## 결론

**연결 업데이트**는 단순한 텍스트 편집을 넘어 **맥락을 이해하는 AI 어시스턴트**입니다:

✅ 텍스트 요약, 확장, 보충  
✅ 수십 개 이미지 중에서 최적 선택  
✅ 레퍼런스 기반 일관된 스타일 이미지 생성  
✅ 복합 작업 자동화

캔버스의 **시각적 연결**이 곧 **AI의 작업 지시서**가 되는 혁신적인 워크플로우입니다.
