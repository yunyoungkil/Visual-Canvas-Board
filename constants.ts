
export const MIN_ZOOM = 0.2;
export const MAX_ZOOM = 2.0;

export const SNAP_THRESHOLD = 6; // in screen pixels
export const HANDLE_SNAP_RADIUS = 10; // in canvas units

export const RESIZE_HANDLE_SIZE = 16; // in screen pixels
export const CONNECTION_HANDLE_SIZE = 12; // in screen pixels
export const CONNECTION_HANDLE_CLICKABLE_AREA = 24; // in screen pixels

export const DUPLICATION_OFFSET = 20; // in canvas units

export const LOCAL_STORAGE_KEY = 'visual-canvas-board-state';

export const GRID_SIZE = 20; // in canvas units

export const CANVAS_WORLD_SIZE = 100000; // A large number for the "infinite" canvas
