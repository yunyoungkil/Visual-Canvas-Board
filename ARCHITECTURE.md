# 시스템 아키텍처

## 개요

비주얼 캔버스 보드는 모든 로직이 클라이언트 측에서 실행되는 순수 프론트엔드 단일 페이지 애플리케이션(SPA)입니다. 별도의 백엔드 서버 없이 Google Gemini API와의 직접적인 통신을 통해 AI 기능을 구현합니다.

## 핵심 구성 요소

### 1. 프론트엔드 (React)
- **프레임워크:** React와 TypeScript를 사용하여 UI를 구축하고 컴포넌트 기반 아키텍처를 따릅니다.
- **상태 관리:** 대부분의 애플리케이션 상태는 최상위 `App` 컴포넌트에서 React Hooks (`useState`, `useRef`, `useCallback`)를 통해 중앙 집중식으로 관리됩니다. 이 방식은 현재 규모에서는 효과적이지만, 향후 기능 확장에 따라 리팩토링이 필요할 수 있습니다. (상세 내용은 `RECOMMENDATIONS.md` 참고)
- **데이터 영속성:** 사용자의 작업 내용은 브라우저의 `localStorage`에 자동으로 저장되어, 페이지를 새로고침해도 작업을 이어서 할 수 있습니다.

### 2. 주요 컴포넌트 구조
- **`App.tsx`**: 애플리케이션의 메인 컨테이너. 모든 상태, 비즈니스 로직, 이벤트 핸들러(드래그, 리사이즈, AI 호출 등)를 포함하는 핵심 파일입니다.
- **`CanvasItemComponent.tsx`**: 캔버스 위의 개별 아이템(텍스트, 이미지, 도형)을 렌더링하고 상호작용을 처리합니다.
- **`ConnectorsLayer.tsx`**: 아이템 간의 연결선을 SVG 레이어로 렌더링합니다.
- **`LeftToolbar.tsx`**: 도구 선택(이동, 텍스트, 도형, AI 기능) UI를 제공합니다.
- **`ContextualActionBar.tsx`**: 아이템 선택 시 나타나는 상황별 메뉴(그룹, 정렬, 삭제 등)를 제공합니다.
- **`DetailsPanel.tsx`**: 선택된 아이템의 세부 속성(위치, 크기, 색상 등)을 편집하고, AI 텍스트 편집 기능을 제공합니다.
- **AI 기능 모달 컴포넌트들** (`AiGenerationBar.tsx`, `OutlineModal.tsx`, `AiChatAssistant.tsx` 등): 각 AI 기능을 위한 독립적인 UI를 제공합니다.

### 3. AI 통합 (Gemini API)
- **SDK:** 공식 `@google/genai` JavaScript SDK를 사용하여 Gemini API와 통신합니다.
- **실행 환경:** 모든 AI API 호출은 사용자의 브라우저(클라이언트 측)에서 직접 이루어집니다. 따라서 API 키는 `process.env.API_KEY` 환경 변수를 통해 안전하게 주입되어야 합니다.
- **기능별 모델 활용:**
    - **텍스트 생성/편집:** `gemini-2.5-flash`, `gemini-2.5-pro`
    - **이미지 생성:** `imagen-4.0-generate-001`
    - **멀티모달 (장면 생성, 이미지 편집):** `gemini-2.5-flash-image`
    - **비디오 생성:** `veo-3.1-fast-generate-preview`
    - **Function Calling:** AI 챗 어시스턴트에서 사용자의 요청을 분석하여 이미지 생성과 같은 특정 함수를 호출하는 데 사용됩니다.

## 데이터 흐름

1.  **사용자 입력:** 사용자가 캔버스와 상호작용(클릭, 드래그, 키보드 입력 등)합니다.
2.  **이벤트 핸들링:** `App.tsx`에 정의된 이벤트 핸들러가 사용자 입력을 처리합니다.
3.  **상태 업데이트:** 핸들러는 `setItems`, `setConnectors` 등의 함수를 호출하여 애플리케이션 상태를 업데이트합니다.
4.  **리렌더링:** 상태 변경으로 인해 관련된 React 컴포넌트들이 다시 렌더링되어 UI에 변경 사항이 반영됩니다.
5.  **(AI 기능의 경우)** AI 관련 버튼 클릭 시, `App.tsx` 내의 해당 함수가 Gemini API를 호출합니다. API 응답을 받은 후, 결과에 따라 상태를 업데이트하여 UI에 반영합니다.
