# 비주얼 캔버스 보드: 향후 로드맵 (Future Roadmap)

## 1. 개요
이 문서는 현재 구현된 비주얼 캔버스 보드의 핵심 기능들을 기반으로, 앞으로 애플리케이션을 더욱 발전시키기 위한 향후 개발 계획을 기술합니다. 현재 구현된 기능에 대한 상세 명세는 `FEATURES.md`와 `README.md`를 참고해 주십시오.

## 2. 향후 로드맵 (Future Roadmap)

### 2.1. AI 챗 어시스턴트 기능 확장: 전체 캔버스 제어
- **목표:** 단순 콘텐츠 생성을 넘어, 대화를 통해 캔버스 객체를 직접 조작하는 기능을 추가합니다. (예: "파란색 사각형들을 모두 왼쪽 상단으로 옮겨줘", "모든 텍스트 항목의 글꼴을 18px로 변경해줘").
- **구현 방안:** Gemini Function Calling 기능을 확장하여 캔버스 아이템의 위치, 스타일, 연결 관계를 변경하는 함수들을 추가하고 모델에 제공합니다.

### 2.2. 실시간 음성 대화 도입 (Gemini Live API)
- **목표:** 키보드 입력 없이 음성만으로 AI 어시스턴트와 대화하며 창의적인 작업을 수행할 수 있는 핸즈프리 환경을 제공합니다.
- **구현 방안:** Gemini Live API를 도입하여 실시간 음성 스트림을 처리하고, 모델의 음성 응답을 사용자에게 재생하는 기능을 구현합니다.

### 2.3. AI 기반 자동 레이아웃 및 디자인 제안
- **목표:** 캔버스 위의 항목들을 AI가 분석하여 의미적으로나 시각적으로 더 나은 배치(예: 타임라인, 순서도, 그리드)를 자동으로 제안하고 적용하는 기능을 추가합니다.
- **구현 방안:** 멀티모달 프롬프트를 통해 캔버스 항목들의 위치와 내용을 Gemini에 전달하고, 개선된 레이아웃 좌표를 JSON 형식으로 반환받아 적용합니다.

### 2.4. 고급 비디오 생성 기능
- **목표:** 기존 텍스트-투-비디오 기능을 넘어, 이미지나 다른 비디오를 기반으로 새로운 비디오를 생성하거나, 기존 비디오를 확장하는 등 `Veo` 모델의 고급 기능을 도입합니다.
- **구현 방안:** `generateVideos` API에 이미지 또는 비디오 객체를 파라미터로 추가하여 호출하는 로직을 구현합니다.

### 2.5. 사용자 스타일 프로필
- **목표:** AI가 생성하는 텍스트(문체, 형식)나 이미지(화풍, 색감)가 사용자의 선호 스타일에 맞춰 일관성 있게 생성되도록 개인화 기능을 추가합니다.
- **구현 방안:** 사용자가 선호 스타일을 설정하거나, 기존 작업물을 기반으로 AI가 스타일을 학습하여 `systemInstruction`에 동적으로 반영하는 시스템을 구축합니다.
